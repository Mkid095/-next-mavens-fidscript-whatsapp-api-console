// Barrel — delegates to ./chatbots/index.js (the new sub-folder router).
// The old inlined handlers live in sub-modules; the router is in chatbots/chatbotRouter.js.
export { default } from './chatbots/index.js';
