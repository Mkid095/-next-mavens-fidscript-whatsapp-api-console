// CampaignBuilderPreview — preview panel for the campaign builder.
// Currently a placeholder: the broadcast form is inline; drip/trigger
// use DripBuilderPanel. Extracted as a dedicated file so the shell
// stays under 60 lines and the preview concern is explicit.
export default function CampaignBuilderPreview() {
  return null;
}
