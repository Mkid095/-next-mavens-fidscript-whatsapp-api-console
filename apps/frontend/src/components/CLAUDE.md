# apps/frontend/src/components — Shared UI Components

## Purpose

Reusable React components used across pages.

## Conventions

- Max 150 lines per file
- One component per file
- Co-locate sub-components in folder (e.g., `Button/` + `Button.tsx` + `Button.css`)

## Related

- [Parent CLAUDE.md](../CLAUDE.md)
